> 本文档是 Git 版本控制系统的系统性学习总结，涵盖从基础概念到团队协作的全链路知识。建议结合实践操作进行学习。

------

## 📚 第一部分：核心概念与工作原理

### 1.1 版本控制的意义

- **什么是版本控制**：记录文件内容变化，以便将来查阅特定版本的系统。
- **为什么需要 Git**：
  - 备份文件修改历史，可随时回退
  - 多人协作开发，解决代码冲突
  - 分支管理，实现功能隔离与并行开发

### 1.2 Git 的三个工作区域





**关键理解**：

- **工作区**：你直接看到和编辑的目录
- **暂存区**：临时存放改动的“缓存区域”
- **本地仓库**：存放所有版本记录的数据库（`.git`文件夹）
- **远程仓库**：托管在服务器上的共享仓库（如GitHub）

### 1.3 文件的四种状态

text

```
未跟踪(Untracked) -- git add --> 已暂存(Staged)
         ↑                               |
         |--- 修改但未暂存(Modified) <---|
```



------

## 💻 第二部分：基础命令与日常操作

### 2.1 仓库生命周期操作

| 阶段         | 命令              | 说明           | 示例                                         |
| :----------- | :---------------- | :------------- | :------------------------------------------- |
| **初始化**   | `git init`        | 初始化新仓库   | `git init my-project`                        |
| **克隆**     | `git clone <url>` | 克隆远程仓库   | `git clone https://github.com/user/repo.git` |
| **状态检查** | `git status`      | 查看工作区状态 | `git status -s` (简洁模式)                   |
| **远程管理** | `git remote -v`   | 查看远程仓库   | `git remote add origin <url>`                |

### 2.2 文件操作命令集

#### 添加与提交

bash

```
# 添加指定文件到暂存区
git add README.md

# 添加所有变化（新文件+修改+删除）
git add .

# 提交到本地仓库
git commit -m "清晰描述本次修改"

# 修改上一次提交（未push时）
git commit --amend -m "新的提交信息"
```



#### 查看与比较

bash

```
# 查看详细提交历史
git log

# 单行显示历史
git log --oneline --graph -10

# 查看工作区与暂存区的差异
git diff

# 查看暂存区与最新提交的差异
git diff --staged
```



#### 删除与移动

bash

```
# 删除文件（同时从工作区和Git中删除）
git rm filename.txt

# 仅从Git删除，保留本地文件
git rm --cached secret-file.txt

# 重命名文件
git mv oldname.txt newname.txt
```



### 2.3 撤销与恢复操作

| 场景                     | 命令                            | 效果                           |
| :----------------------- | :------------------------------ | :----------------------------- |
| 丢弃工作区修改           | `git checkout -- filename`      | 文件恢复到最后一次提交状态     |
| 取消暂存                 | `git reset HEAD filename`       | 从暂存区移回工作区             |
| 撤销最近提交（保留修改） | `git reset --soft HEAD~1`       | 回退提交，修改保留在暂存区     |
| 彻底撤销提交             | `git reset --hard HEAD~1`       | **谨慎使用**：彻底丢弃最近提交 |
| 恢复已删除文件           | `git checkout HEAD -- filename` | 从最近提交恢复文件             |

------

## 🌿 第三部分：分支管理与工作流

### 3.1 分支基础操作

bash

```
# 查看所有分支（*表示当前分支）
git branch -av

# 创建新分支
git branch feature-auth

# 切换分支
git checkout main
git checkout feature-auth

# 创建并切换分支（推荐）
git checkout -b hotfix-urgent

# 删除已合并分支
git branch -d old-feature
git branch -D force-delete  # 强制删除未合并分支

# 重命名当前分支
git branch -m new-branch-name
```



### 3.2 合并与变基

#### 合并（Merge）

bash

```
# 将feature分支合并到当前分支
git checkout main
git merge feature-auth -m "合并用户认证功能"

# 合并时总是生成合并提交
git merge --no-ff feature-auth
```



#### 变基（Rebase）

bash

```
# 将当前分支变基到main分支
git checkout feature-auth
git rebase main

# 交互式变基（修改历史）
git rebase -i HEAD~3
```



### 3.3 主流工作流模型

#### GitHub Flow（轻量简单）

1. `main`分支始终保持可部署状态
2. 从`main`创建功能分支：`git checkout -b feature-x`
3. 频繁提交到功能分支
4. 创建Pull Request进行代码审查
5. 审查通过后合并到`main`
6. 立即部署

#### Git Flow（企业级严谨）

bash

```
# 主要分支
main        # 生产版本
develop     # 开发集成

# 支持分支
feature/*   # 功能开发（从develop拉取）
release/*   # 版本准备（从develop拉取）
hotfix/*    # 紧急修复（从main拉取）
```



------

## ☁️ 第四部分：远程协作与GitHub

### 4.1 远程仓库操作

bash

```
# 添加远程仓库
git remote add origin https://github.com/user/repo.git

# 查看远程仓库
git remote -v

# 推送本地分支到远程
git push -u origin main  # 首次推送使用 -u
git push                 # 后续推送

# 拉取远程更新
git pull origin main     # = fetch + merge
git fetch origin         # 仅获取不合并

# 跟踪远程分支
git checkout -b feature origin/feature

# 删除远程分支
git push origin --delete old-branch
```



### 4.2 解决推送冲突

bash

```
# 1. 先拉取最新代码
git pull origin main

# 2. 解决冲突（编辑标记冲突的文件）
# <<<<<<< HEAD
# 你的代码
# =======
# 远程代码
# >>>>>>> commit-hash

# 3. 重新提交并推送
git add .
git commit -m "解决合并冲突"
git push
```



### 4.3 SSH vs HTTPS 认证

| 方面     | SSH                            | HTTPS                              |
| :------- | :----------------------------- | :--------------------------------- |
| 地址格式 | `git@github.com:user/repo.git` | `https://github.com/user/repo.git` |
| 认证方式 | SSH密钥对                      | 用户名+密码/个人访问令牌           |
| 首次配置 | 需生成并添加公钥               | 直接使用                           |
| 后续使用 | 无需每次输入                   | 需输入凭据（可缓存）               |
| 推荐场景 | 个人常用设备                   | 临时设备/CI/CD环境                 |

**配置SSH密钥**：

bash

```
# 1. 生成密钥
ssh-keygen -t ed25519 -C "your-email@example.com"

# 2. 查看并复制公钥
cat ~/.ssh/id_ed25519.pub

# 3. 添加到GitHub：Settings → SSH and GPG keys

# 4. 测试连接
ssh -T git@github.com
```



------

## 🛠️ 第五部分：高级技巧与最佳实践

### 5.1 高效的 .gitignore 配置

gitignore

```
# 操作系统文件
.DS_Store
Thumbs.db

# 编辑器/IDE
.vscode/
.idea/
*.swp

# 语言特定
# Python
__pycache__/
*.py[cod]
.env
venv/

# Node.js
node_modules/
npm-debug.log*

# 日志与构建产物
*.log
build/
dist/
```



### 5.2 交互式暂存与提交

bash

```
# 交互式添加（选择部分修改）
git add -p

# 交互式提交（分别提交不同修改）
git commit -p

# 修改最近几次提交（变基）
git rebase -i HEAD~3
# 命令选项：pick, reword, edit, squash, fixup, drop
```



### 5.3 标签管理与版本发布

bash

```
# 创建轻量标签
git tag v1.0.0

# 创建附注标签（推荐）
git tag -a v1.0.0 -m "正式发布版本1.0.0"

# 推送标签到远程
git push origin v1.0.0
git push origin --tags  # 推送所有标签

# 查看标签
git tag -l "v1.*"
```



### 5.4 找回丢失的提交

bash

```
# 查看所有操作记录（包括已删除的提交）
git reflog

# 恢复特定的提交
git checkout <commit-hash>

# 创建新分支指向丢失的提交
git branch recovery-branch <commit-hash>
```



------

## 📖 第六部分：学习路径建议

### 6.1 渐进式学习计划

| 阶段      | 学习重点                 | 实践项目                   |
| :-------- | :----------------------- | :------------------------- |
| **第1周** | 基础概念、单人工作流     | 个人日记项目，每日提交     |
| **第2周** | 分支管理、合并冲突       | 开发简单网站，使用功能分支 |
| **第3周** | GitHub协作、Pull Request | 参与开源项目，提交PR       |
| **第4周** | 高级话题、工作流规范     | 团队模拟项目，实践Git Flow |